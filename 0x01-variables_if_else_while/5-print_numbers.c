#include <stdio.h>

/**
*
*main -  a program that prints all single digit numbers of base 10.
*
*Return: Always 0 (Successfull)
*/
int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
printf("%d", i);
}
putchar('\n');
;
return (0);
}
